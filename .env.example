# ============================================
# DJ Mixing Platform - Environment Variables
# ============================================
# This file contains all configuration options for the application.
# Copy this file to .env and customize the values as needed.
#
# Usage:
#   cp .env.example .env
#   # Then edit .env with your settings

# ============================================
# Database Configuration
# ============================================
# PostgreSQL database settings
POSTGRES_DB=djmixing
POSTGRES_USER=djuser
POSTGRES_PASSWORD=djpassword

# Database URL (for backend application)
# Format: postgresql://[user]:[password]@[host]:[port]/[database]
# For Docker: DATABASE_URL=postgresql://djuser:djpassword@db:5432/djmixing
# For Local Dev: DATABASE_URL=postgresql://djuser:djpassword@localhost:5432/djmixing

# ============================================
# Backend Configuration
# ============================================
# Secret key for JWT tokens and encryption
# IMPORTANT: Change this in production! Generate with: openssl rand -hex 32
SECRET_KEY=your-secret-key-change-in-production

# Upload directory for audio files
# For Docker: Use /app/uploads
# For Local Dev: Use ./uploads or an absolute path
UPLOAD_DIR=/app/uploads

# Redis URL for caching
# For Docker: REDIS_URL=redis://redis:6379/0
# For Local Dev: REDIS_URL=redis://localhost:6379/0
REDIS_URL=redis://localhost:6379/0

# ============================================
# Spotify API (Optional)
# ============================================
# Get credentials from: https://developer.spotify.com/dashboard
# These are optional - leave empty if not using Spotify integration
SPOTIFY_CLIENT_ID=
SPOTIFY_CLIENT_SECRET=

# ============================================
# Frontend Configuration
# ============================================
# Backend API URL that the frontend should connect to
# 
# ** IMPORTANT: Choose the correct value for your environment **
#
# For Docker Compose (recommended):
#   REACT_APP_API_URL=http://localhost:8000
#   (Frontend in browser connects to backend via host machine's localhost)
#
# For Local Development:
#   REACT_APP_API_URL=http://localhost:8000
#   (Both frontend and backend running locally)
#
# For Production:
#   REACT_APP_API_URL=https://your-domain.com
#   (Use your actual domain)
#
REACT_APP_API_URL=http://localhost:8000

# ============================================
# Troubleshooting
# ============================================
# If you see "Network Error" in the frontend:
# 1. Ensure the backend is running:
#    - Docker: docker-compose ps (should show backend as "Up")
#    - Local: Check if uvicorn is running on port 8000
#
# 2. Verify backend health:
#    curl http://localhost:8000/health
#
# 3. Check REACT_APP_API_URL is correct:
#    - Should match where the backend is accessible
#    - For Docker, always use localhost (NOT the service name)
#    - React apps run in browser, not in container
#
# 4. Check firewall/network settings:
#    - Ports 3000 (frontend) and 8000 (backend) should be accessible
#
# 5. Check backend logs:
#    docker-compose logs backend
#
# For more help, see: README.md (Troubleshooting section)

